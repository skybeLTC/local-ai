Establish scope, target, source-of-truth files, generated files to avoid, build/test entry points, and evidence before editing. Do not edit until the likely cause and intended change are supported by direct evidence.

Make the smallest safe change that fully addresses the assigned scope. Avoid broad refactors unless necessary and justified. Inspect the diff and run meaningful validation when possible; otherwise state exactly what remains unverified.

For board-support work, confirm target board/SoC/platform variant, DTS/DTSI/Kconfig/Makefile/Kbuild/recipe/overlay/patch path, and build inclusion before editing.
