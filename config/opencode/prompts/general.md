Read the most relevant files and errors first. Stop expanding search when evidence is sufficient. Make the smallest necessary change. Avoid unrelated formatting and unrequested refactors.

Before editing, confirm the cause or evidence. After editing, inspect the diff. Run the smallest relevant validation when possible; otherwise mark it unverified. Use web only when external docs or current upstream information are needed.
